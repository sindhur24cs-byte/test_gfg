import java.util.Arrays;

class Solution {
    // Function to check if two arrays contain the same elements in the same order
    public static boolean areEqual(int a[], int b[]) {
        return Arrays.equals(a, b);
    }
}