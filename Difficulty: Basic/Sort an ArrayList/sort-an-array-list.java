import java.util.ArrayList;
import java.util.Collections;

class Solution {
    // Function to sort an ArrayList in increasing order
    public static void sortArrayList(ArrayList<Integer> list) {
        Collections.sort(list);
    }
}