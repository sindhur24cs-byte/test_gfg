import java.util.Collections;
import java.util.LinkedList;

class Solution {
    // Function to fill all elements of the LinkedList with value k
    public static void fillLinkedList(LinkedList<Integer> ll, int k) {
        Collections.fill(ll, k);
    }
}